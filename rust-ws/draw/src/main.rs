use embedded_hal::digital::v2::OutputPin;
use embedded_graphics::prelude::*;
use embedded_graphics::primitives::{Line};

use embedded_graphics::pixelcolor::BinaryColor;

pub fn draw(display: GraphicsMode<_>) {
    println!("Hello, world!");
}
